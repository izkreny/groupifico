<% content_for :title, "New group" %>

<%# The one pushed screen outside any group, so the header's leading slot holds a back chevron the
    layout resolves to the groups index rather than to a group's home. %>
<%= render "shared/page_header", title: "New group" %>

<div class="w-full">
  <%= render "form", group: @group %>
</div>
