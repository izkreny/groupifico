<% content_for :title, "New address" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New address</h1>

  <%= render "form", address: @address %>

  <%= link_to "Back to addresses", addresses_path, class: "btn btn-ghost" %>
</div>
